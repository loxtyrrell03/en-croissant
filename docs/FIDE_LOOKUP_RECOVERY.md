# FIDE search recovery

Current verification update (2026-09-12): the historical primary elapsedMs mismatch below is resolved at its native Specta export, preserving the existing JSON number contract. Both checkouts now pass TypeScript and the production frontend build. The native contract regression and eight import utility tests pass; see docs/PHONE_OTB_LIFECYCLE.md for the scoped evidence. This does not add installed-app or live-network verification to the earlier FIDE fixtures.

The desktop and phone import pickers distinguish a failed lookup from a confirmed empty result. Errors have an adjacent Retry search action. Clearing, shortening, disabling or unmounting the picker abandons its pending request; replacement queries cannot select old suggestions. Desktop results open only while the input has focus. Suggestions retain complete names, labelled rating types and FIDE IDs, and the desktop list uses its available viewport height.

Desktop lookups use the existing shared Lichess request lane, including its spacing and rate-limit cooldown. Direct lookup and ranked search share a bounded cache and pending request. Cancelling one reader preserves other readers; cancelling the last reader abandons the transport. The eight-second deadline includes queueing and body reads, including a provider promise that ignores abort. Only successful responses and confirmed misses are cached. The existing full-career year helper and tolerant name ranking remain in the common player model. The unused older prep picker was removed after checking all source consumers.

The PC search service retains its existing success/miss TTLs, photo metadata and name ranking. It now limits the cache to 128 entries, serializes searches with 500 ms spacing, respects at least a minute after HTTP 429, validates response shape and exact numeric identity, and bounds queued/body reads to eight seconds. A disconnected HTTP reader cancels only its own search subscription. The phone transport validates the PC response and has a ten-second deadline, allowing the PC's eight-second failure to arrive first. These are process-local request lanes; they are not a cross-process traffic coordinator.

Both import forms use the same identity resolver. A supplied ID must resolve to that exact player; conflicting IDs, missing identities and failed ID lookups prevent import. Duplicate exact-name matches never silently pin the first ID. Full-name imports without an ID remain supported when optional public-mirror enrichment fails. An explicit import year remains authoritative; otherwise a resolved birth year reaches the same request. ID autofill is cancelled by editing, selecting another player, starting preflight or unmounting. Stop FIDE search cancels preflight before a native collection or PC job starts. Existing collection, conversion, job cancellation and saved-data behavior remain separate.

## Verification on 2026-09-12

- Both the isolated integration checkout and development checkout passed 42 focused frontend tests and 35 FIDE/OTB service tests. Thirteen new frontend and seven new service regressions cover failure recovery, shape/identity validation, deadlines, shared cancellation, rate limiting and import identity handling.
- `scripts/qa-fide-recovery.mjs` passed 18 grouped real-component cases in each checkout. It renders the actual common desktop/phone picker and both import panels with dedicated provider/native module fixtures. Coverage includes retry, confirmed misses, stale selection, input changes, disabled/unmounted readers, exact-ID preflight, conflicts, birth-year propagation, autofill ordering, Stop, and keyboard selection. Layouts cover 1100/760/360 px and 100/200% scale, plus a 360×600 px keyboard/scroll case. There were no page errors or external requests. Retained source at `b9ab5156` failed 12 of these 18 groups; the other six already worked.
- The isolated checkout passed TypeScript and targeted lint with no warnings. The full production frontend and OTB/review worker builds passed. Its inherited package-manager shim points to an obsolete worktree path; the same build stages were run through the installed Node executables. Build output retained existing bundle-size/plugin-time warnings. Generated path/EOL-only changes were verified and excluded from the commit.
- The development checkout's TypeScript check remains affected by an unrelated existing mismatch: unchanged `src/utils/tests/otbGameImport.test.ts:97` supplies numeric `elapsedMs`, while its modified generated bindings declare `bigint`. Those concurrent bindings/test contracts were preserved. Its focused tests and both rendered target flows pass; do not call that whole-checkout type check successful.

Evidence is in `tmp/fide-recovery/` in each checkout. Primary before-files are under `tmp/fide-recovery/before/`; test backups have a `.before` suffix so they cannot be collected as current tests. Set `FIDE_QA_DEPENDENCIES` to a package.json whose environment provides Playwright when running the optional renderer harness.

This is source/build/fixture evidence. No live provider lookup, owner database mutation, installed-app interaction, native collector acceptance, PC-service restart, phone deployment or physical-device verification was performed. Novelty's corresponding request/picker/preflight changes are on its release-readiness branch. The subsequent profile milestone below records completion of the applicable desktop profile source changes. The wider release audit is incomplete.


## FIDE profiles and launch controls

The separate desktop profile modal now uses the same bounded search service. A unique result opens directly; multiple matches require an explicit choice, displaying FIDE ID, available federation/birth year and labelled rating data. Users can refine the query or retry without editing the game. Closing, changing the source identity or replacing a request prevents late profile results. Returning to matches restores the selected row's keyboard focus. Board/Game Info callers pass the standard WhiteFideId/BlackFideId tags preserved in GameHeaders.other; a valid ID takes precedence over a blank or ambiguous name. The unused first-hit getFidePlayer path was removed after checking all consumers.

White/Black and board-name controls are keyboard buttons, and the personal-card icon has an accessible name. A missing board name retains a visible Player action. Profile links use the existing Tauri opener with visible failure/retry, duplicate-click protection and rejection of late errors after switching profiles or closing. Plain-web anchors retain their normal target/rel behavior. Rating cards wrap to available width instead of clipping at larger text sizes. Existing profile artwork/country styling remains.

Both checkouts passed the ten real-component groups in scripts/qa-fide-profile.mjs, including ambiguity/back, refinement, miss/error/retry, old-request cancellation, close/reopen/prop replacement, native-link request ownership, browser fallback, blank/ID-only headers, actual Game Info header IDs/preservation and keyboard board-bar entry. Match/detail/rating layouts cover 1100/760/360px and 100/200% scale; focus remains in the modal and screenshots wait for transitions. The final renderer reports no page errors or external requests. All ten current assertion groups failed on retained source at 9d72e288; some failures reflect missing accessible link/search names rather than ten independent behavioral defects. Visual review caught and fixed rating-card clipping that page-level overflow checks had missed. Both checkouts passed 26 focused FIDE tests; isolated TypeScript, touched-source lint and the production frontend build passed. The primary elapsedMs number/bigint type mismatch recorded above remains unchanged.

These are real components with dedicated provider/opener/context fixtures. The Game Info fixture is read-only, and the full Board/PersonalCard workspaces were not rebuilt inside this profile harness; their scoped call-site changes passed isolated TypeScript/build and source review. No OS browser was opened, no live FIDE request was sent, and no installed app or phone service was restarted. The phone companion has no equivalent profile-modal surface (its FIDE lookup is the already-verified import picker), so no new phone view was introduced. This completes the applicable profile source parity with Novelty; installed/runtime verification and the wider product release audit remain separate.
